name: Deploy HAVEN prototype

on:
  push:
    branches: [main]
    paths:
      - 'prototype/haven-demo/**'
      - '.github/workflows/deploy-haven-demo.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload HAVEN prototype
        uses: actions/upload-pages-artifact@v4
        with:
          path: prototype/haven-demo
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
